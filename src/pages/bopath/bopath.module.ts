import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { BopathPage } from './bopath';

@NgModule({
  declarations: [
    BopathPage,
  ],
  imports: [
    IonicPageModule.forChild(BopathPage),
  ],
})
export class BopathPageModule {}
